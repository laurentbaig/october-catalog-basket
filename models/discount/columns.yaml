# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    active:
        label: Active
        type: switch
    product:
        label: Product
        relation: product
        select: name
        searchable: true
    amount:
        label: Amount
        type: number
    since:
        label: From
        type: datetime
    until:
        label: Until
        type: datetime
    ordersCount:
        label: Order Count
        type: number
